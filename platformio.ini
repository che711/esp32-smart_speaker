[env:esp32-devkit]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/53.03.10/platform-espressif32.zip
board       = esp32dev
framework   = arduino

upload_speed  = 921600
monitor_speed = 115200

board_build.filesystem = littlefs
board_build.partitions = partitions.csv

; patch_audio.py исправляет все несовместимости ESP32-audioI2S с IDF 5.3
extra_scripts = pre:patch_audio.py

lib_deps =
    https://github.com/schreibfaul1/ESP32-audioI2S.git
    esphome/ESPAsyncWebServer-esphome @ ^3.0.0
    mathieucarbou/AsyncTCP @ ^3.1.4
    adafruit/Adafruit SSD1306 @ ^2.5.10
    adafruit/Adafruit GFX Library @ ^1.11.9
    bblanchon/ArduinoJson @ ^7.0.0
    lorol/LittleFS_esp32 @ ^1.0.6

; ВАЖНО: build_unflags убран — он ломал компиляцию C файлов в библиотеках
; -std=gnu++2a применяется поверх дефолтного gnu++17 на GCC 13
build_flags =
    -DCORE_DEBUG_LEVEL=0
    -std=gnu++2a
    -DI2S_BCLK=27
    -DI2S_LRC=26
    -DI2S_DOUT=25
    -DI2C_SDA=21
    -DI2C_SCL=22
